define command {
    command_name        restart-shinken
    # See reload-shinken.cfg: same action, no init.d script in this
    # containerized deployment. Kill the arbiter, let
    # "restart: unless-stopped" bring the container back.
    command_line        /usr/bin/pkill -TERM -f shinken-arbiter
}
