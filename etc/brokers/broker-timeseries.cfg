# Reserved for the experimental timeseries integration.
# No broker or module is enabled here: broker_timeseries is not implemented.
# This directory is loaded recursively by etc/shinken.cfg; every active
# definition must describe a supported Shinken object, never an INI section.
# See doc/METRICS.md for the current implementation status.
